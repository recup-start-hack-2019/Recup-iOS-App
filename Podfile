target 'Recup' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Recup
  pod 'SwiftyJSON', '~> 4.0'
  pod 'BitcoinKit'
  pod 'KeychainAccess'
  pod "QRCode"
  pod 'Alamofire', '~> 4.0'
  pod 'SwiftMessages'

  target 'RecupTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RecupUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
